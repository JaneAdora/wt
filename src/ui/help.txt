wt :: Worktree Wizard

THE LAYOUT
  Two views, toggled with v:

  Tree view (default):
    WORKTREES (top)         : your projects and their worktrees
    SESSIONS  (bottom)      : Claude sessions attached to the selected worktree
  Sessions view:
    SESSIONS · all (top)    : every Claude session on the machine, grouped
                              by cwd.
                                ▼/▸ = group expanded / collapsed
                                ●   = wt found a worktree for this cwd
                                ○   = orphan dir (sessions but no wt match)
    SELECTED SESSION (bot)  : compact detail of the selected session; press
                              Enter for the full-text modal.
  The focused pane (Tree view) has a bright pink border and magenta title.

VIEW
  v  or  V            Toggle between Tree and Sessions views
                      In Sessions view, groups are collapsible just like
                      projects: Enter/Right toggle, Left collapses or jumps
                      to parent group, e/E expand-all/collapse-all.
                      / filter matches against group cwd OR session
                      summary/intent/id. Sessions view shows every session
                      in the current time window (no per-group cap).

SWITCHING PANES
  1                   Focus the WORKTREES pane
  2                   Focus the SESSIONS pane
  Tab  /  Shift-Tab   Cycle focus between the two panes
                      (Both directions are the same when there are 2 panes.
                      Use 1 / 2 if Tab is awkward on your terminal.)

NAVIGATION (within the focused pane)
  ↑ ↓  or  j k       Move selection up/down
  ← →  or  h l       Tree navigation (worktrees pane):
                        Right on collapsed project: expand
                        Right on expanded project:  step into first worktree
                        Right on worktree row:      focus the SESSIONS pane
                        Left  on expanded project:  collapse
                        Left  on worktree row:      jump up to parent project
  Enter               Worktrees pane:
                        Project row: expand/collapse
                        Worktree row: focus the SESSIONS pane
                      Sessions pane:
                        Open a detail modal with the full untruncated text
                        (id, cwd, status, date, summary/intent). Use this
                        when text is clipped on the right.
                        For commit subjects, press g instead.
  e  or  E            Expand all / collapse all (toggle). Aliases.

ACTIONS
  c                   Copy launch command to clipboard (OSC 52); stay open
  o                   Open: copy to clipboard AND print to terminal, then exit.
                      Use this when you're ready to actually launch.
  d  or  D            Same as o, plus appends --dangerously-skip-permissions.
                      Aliases (lowercase / uppercase are the same key; bound
                      both so speech-to-text and caps-lock don't trip you up).

LAUNCH COMMAND SHAPE
  Worktree selected:  cd PATH && claude
  Interactive sess:   cd PATH && claude --resume ID
  Background job:     cd PATH && claude
  (Append --dangerously-skip-permissions when using `d` instead of `o`/`c`.)

VIEWS
  r                   Refresh (preserves selection, filter, search, expansion)
  /                   Filter by substring; type, Enter to apply, Esc to cancel
  a                   Toggle active-only filter (default on)
  t  or  T            Cycle interactive-session window: 30d > all > 7d. Aliases.
  g                   Show commit log for the selected worktree
  ?                   Show this help

DELETE (soft, moves to ~/.config/wt/trash/)
  x                   Soft-delete the selected background job. Two-press
                      confirm: first arms, second within 3s moves the job
                      directory into the trash. Refuses while Running.
  X                   Bulk-delete all Completed and Failed bg jobs on the
                      currently selected worktree. Two-press confirm.
                      Running jobs are skipped automatically.
  u  or  U            Undo the most recent soft-delete (single or bulk).
                      Restores from ~/.config/wt/trash/. LIFO; last
                      deleted item comes back first.

EXIT
  q  or  Ctrl-C       Quit (saves UI state)

IN A MODAL (log or help)
  j k  or  ↑ ↓        Scroll one line
  Space / PgDn        Page down
  b / PgUp            Page up
  g / Home            Jump to top
  G / End             Jump to bottom
  Esc / q / Enter     Close modal

LEAVING SEARCH (the / mode)
  Esc                 Cancel; the filter clears
  Enter               Apply; the filter sticks until you press / again

DISCOVERY SCOPE
  By default, wt scans the immediate non-hidden subdirs of:
    ~/                       (catches repos at home root, e.g., ~/peon-ping)
    ~/projects/
    ~/Projects/
    ~/Projects/clients/
  Each one is included only if it contains a .git. Linked worktrees
  are picked up via `git worktree list` per repo, so they show up
  no matter where they live on disk.

  Override with WT_ROOTS env var, colon-separated (like PATH):
    WT_ROOTS=~/projects:~/Projects:~/code wt

PERSISTENT STATE
  UI preferences (filter, expansion set, search, selection, time window)
  are saved to ~/.config/wt/state.json on quit and restored on launch.

ABOUT
  Single-host dashboard. Built for one terminal tab, designed for mobile
  SSH (Blink, Termius) via OSC 52 clipboard and width-collapsing.
